---
title: Magick Image Converter
emoji: 🖼️
colorFrom: blue
colorTo: green
sdk: docker
app_port: 8000
pinned: false
---

# Magick Image Converter

This Hugging Face Space is a generated thin wrapper. The product source of truth is [BlueSkyXN/ImageMagickAPI-HFS](https://github.com/BlueSkyXN/ImageMagickAPI-HFS). Its Dockerfile clones and detached-checks out the exact full Git commit embedded in the generated `Dockerfile`; it never copies application files from the Space context.

Only these generated wrapper files are published: `.dockerignore`, `BUILD_SOURCE.txt`, `Dockerfile`, `README.md`, and `hfs-dev.toml`. Publish only through the guarded manual GitHub Actions workflow after reviewing the exact commit provenance.
